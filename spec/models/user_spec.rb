require 'rails_helper'

RSpec.describe User, type: :model do
  # let!(:user) { create :user }
  # describe '#balance' do
  #   let!(:plus_operation) { create :operation, user: user, value: 40 }
  #   let!(:minus_operation) { create :operation, user: user, value: -20 }
  #   subject { user.balance }
  #   it { is_expected.to eq(20) }
  # end
end
